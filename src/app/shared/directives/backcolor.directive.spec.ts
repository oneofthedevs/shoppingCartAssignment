import { BackcolorDirective } from './backcolor.directive';

describe('BackcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackcolorDirective();
    expect(directive).toBeTruthy();
  });
});
